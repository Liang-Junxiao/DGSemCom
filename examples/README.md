# Examples

Example image folders are not included in this preliminary release.

The dataloader expects local image folders such as:

```text
examples/train
examples/test
examples/visual
```

You can create these folders locally or override the paths from the command line.
